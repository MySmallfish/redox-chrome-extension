{
  "name": "redox-property-tracker",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "node scripts/build.js --watch",
    "build": "node scripts/build.js",
    "package": "node scripts/package-extension.mjs",
    "deploy:chrome-store": "node scripts/deploy-chrome-webstore.mjs",
    "test": "vitest run",
    "test:e2e": "node scripts/build.js && playwright test",
    "lint": "eslint .",
    "format": "prettier --write ."
  },
  "dependencies": {
    "xstate": "^4.38.3"
  },
  "devDependencies": {
    "@playwright/test": "^1.42.0",
    "esbuild": "^0.20.2",
    "eslint": "^9.2.0",
    "jsdom": "^24.0.0",
    "prettier": "^3.2.5",
    "sharp": "^0.34.5",
    "vitest": "^1.3.0"
  }
}
